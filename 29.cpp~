#include <string>

#define MAX_INT 0x8FFFFFFF

using namespace std;

int divide(int dividend, int divisor) {
  int ans = 0;
  while(dividend > 0) {
    dividend -= divide;
    ans++;
    if(ans == MAX_INT) break;
  }

  return ans;
}
